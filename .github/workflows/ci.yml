name: Basic CI
on: 
  push: 
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: print hello
        run: echo "Hello Tanishka, CI is working" 
      - name: Show date
        run: date
      - name: Who triggered
        run: echo "Triggred by ${{ github.actor }}"
